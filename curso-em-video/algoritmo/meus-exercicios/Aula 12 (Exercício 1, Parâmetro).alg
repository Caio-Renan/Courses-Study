algoritmo "Soma por parâmetros"
var
   X, Y: INTEIRO
procedimento SOMA(A, B: INTEIRO)
inicio
      ESCREVAL("Recebi o valor ",A)
      ESCREVAL("Recebi o valor ",B)
      ESCREVAL("A soma entre os dois é igual a ",A+B)
fimprocedimento
inicio
      X <- 5
      Y <- 3
      SOMA(X, Y)
fimalgoritmo
