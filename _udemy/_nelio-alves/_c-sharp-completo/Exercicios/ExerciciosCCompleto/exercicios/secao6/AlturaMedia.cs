using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace ExerciciosCCompleto.exercicios.secao6 {
    internal class AlturaMedia {
        //static void Main(string[] args) {
        //    CultureInfo CI = CultureInfo.InvariantCulture;
        //    double somaVetores = 0;

        //    int n = int.Parse(Console.ReadLine());

        //    double[] vetor = new double[n];

        //    for (int i = 0; i < n; i++) {
        //        vetor[i] = double.Parse(Console.ReadLine(), CI);
        //        somaVetores += vetor[i];
        //    }

        //    double mediaAlturas = somaVetores / (double)n; //Não precisava do (double), fiz para lembrar que isso existe

        //    Console.WriteLine("\nMEDIA DAS ALTURAS = " + mediaAlturas.ToString("F2", CI) + "\n");
        //}
    }
}
